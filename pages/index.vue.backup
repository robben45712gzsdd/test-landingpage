<template>
  <div class="landing-page">
    <!-- Header -->
    <header class="header-section">
      <nav class="navbar">
        <div class="logo">NEKSOLUTION</div>
        <div class="nav-links">
          <a href="#home" class="nav-link">Home</a>
          <a href="#games" class="nav-link">Games</a>
          <a href="#about" class="nav-link">About</a>
        </div>
      </nav>
    </header>

    <!-- Section Navigation Dots -->
    <div class="section-nav-dots">
      <button 
        v-for="(dot, index) in sectionDots" 
        :key="index"
        class="nav-dot"
        :class="{ active: dot.active }"
        :title="dot.label"
        @click="scrollToSection(dot.id)"
      ></button>
    </div>

    <!-- Section Label -->
    <div class="section-label">
      <span class="label-text">{{ currentSectionLabel }}</span>
    </div>

    <!-- HOME SECTION -->
    <section class="hero-section" id="home" ref="heroSection">
      <div class="fixed-video-container">
        <video
          ref="heroVideo"
          class="bg-video"
          :src="videos[0]"
          muted
          playsinline
          autoplay
          loop
          preload="metadata"
        ></video>
        <div class="video-overlay"></div>
      </div>
      <div class="hero-content">
        <div class="hero-text-center">
          <h1 class="hero-title">Entertain The World</h1>
          <p class="hero-description">
            Experience next-generation hyper-casual gaming with stunning visuals and instant gameplay.
          </p>
        </div>
      </div>
    </section>

    <!-- GAMES SHOWCASE SECTION (WITH SCROLL) -->
    <section class="games-showcase" id="games" ref="gamesShowcase">
      <!-- Fixed Video Background for Games -->
      <div class="games-fixed-video-container">
        <video
          ref="gamesVideo"
          class="bg-video"
          :src="videos[0]"
          muted
          playsinline
          autoplay
          loop
          preload="metadata"
        ></video>
        <div class="video-overlay"></div>
      </div>

      <!-- Left Sidebar with Game Cards -->
      <div class="games-sidebar-panel">
        <div class="games-sidebar-content" ref="gamesSidebarContent">
          <!-- Game 1 -->
          <div class="game-card active" ref="gameCard1">
            <div class="game-logo">
              <span class="logo-text">MOB</span>
              <span class="logo-control">CONTROL</span>
            </div>
            <h2 class="game-description">
              The always fresh, oddly satisfying strategy game where anyone can feel skilled and powerful.
            </h2>
            <div class="download-buttons">
              <a href="https://apps.apple.com" class="download-btn app-store" target="_blank">
                <svg class="btn-icon" viewBox="0 0 24 24" fill="currentColor">
                  <path d="M17.05 20.28c-.98.95-2.05.88-3.08.4-1.09-.5-2.08-.48-3.24 0-1.44.62-2.2.44-3.06-.4C2.79 15.25 3.51 7.59 9.05 7.31c1.35.06 2.29.38 3.02.91.99-.72 2.45-.84 3.02-.91 5.54.28 6.26 7.94 2.96 12.97zM12.03 7.25c-.15-2.23 1.66-4.07 3.74-4.25.29 2.58-2.34 4.5-3.74 4.25z"/>
                </svg>
                App Store
              </a>
              <a href="https://play.google.com/store" class="download-btn play-store" target="_blank">
                <svg class="btn-icon" viewBox="0 0 24 24" fill="currentColor">
                  <path d="M3.45 2.426c-.094.148-.15.32-.15.504v7.636h16.8V2.93c0-.184-.056-.356-.15-.504m-16.35.504c0-.832.65-1.506 1.45-1.506h15.1c.8 0 1.45.674 1.45 1.506"/>
                </svg>
                Google Play
              </a>
            </div>
          </div>

          <!-- Game 2 -->
          <div class="game-card" ref="gameCard2">
            <div class="game-logo">
              <span class="logo-text">STACK</span>
              <span class="logo-control">BLAST</span>
            </div>
            <h2 class="game-description">
              Stack colorful blocks and blast them away in this addictive puzzle adventure.
            </h2>
            <div class="download-buttons">
              <a href="https://apps.apple.com" class="download-btn app-store" target="_blank">
                <svg class="btn-icon" viewBox="0 0 24 24" fill="currentColor">
                  <path d="M17.05 20.28c-.98.95-2.05.88-3.08.4-1.09-.5-2.08-.48-3.24 0-1.44.62-2.2.44-3.06-.4C2.79 15.25 3.51 7.59 9.05 7.31c1.35.06 2.29.38 3.02.91.99-.72 2.45-.84 3.02-.91 5.54.28 6.26 7.94 2.96 12.97zM12.03 7.25c-.15-2.23 1.66-4.07 3.74-4.25.29 2.58-2.34 4.5-3.74 4.25z"/>
                </svg>
                App Store
              </a>
              <a href="https://play.google.com/store" class="download-btn play-store" target="_blank">
                <svg class="btn-icon" viewBox="0 0 24 24" fill="currentColor">
                  <path d="M3.45 2.426c-.094.148-.15.32-.15.504v7.636h16.8V2.93c0-.184-.056-.356-.15-.504m-16.35.504c0-.832.65-1.506 1.45-1.506h15.1c.8 0 1.45.674 1.45 1.506"/>
                </svg>
                Google Play
              </a>
            </div>
          </div>

          <!-- Game 3 -->
          <div class="game-card" ref="gameCard3">
            <div class="game-logo">
              <span class="logo-text">RUSH</span>
              <span class="logo-control">HOUR</span>
            </div>
            <h2 class="game-description">
              Navigate through endless traffic in this fast-paced arcade racing challenge.
            </h2>
            <div class="download-buttons">
              <a href="https://apps.apple.com" class="download-btn app-store" target="_blank">
                <svg class="btn-icon" viewBox="0 0 24 24" fill="currentColor">
                  <path d="M17.05 20.28c-.98.95-2.05.88-3.08.4-1.09-.5-2.08-.48-3.24 0-1.44.62-2.2.44-3.06-.4C2.79 15.25 3.51 7.59 9.05 7.31c1.35.06 2.29.38 3.02.91.99-.72 2.45-.84 3.02-.91 5.54.28 6.26 7.94 2.96 12.97zM12.03 7.25c-.15-2.23 1.66-4.07 3.74-4.25.29 2.58-2.34 4.5-3.74 4.25z"/>
                </svg>
                App Store
              </a>
              <a href="https://play.google.com/store" class="download-btn play-store" target="_blank">
                <svg class="btn-icon" viewBox="0 0 24 24" fill="currentColor">
                  <path d="M3.45 2.426c-.094.148-.15.32-.15.504v7.636h16.8V2.93c0-.184-.056-.356-.15-.504m-16.35.504c0-.832.65-1.506 1.45-1.506h15.1c.8 0 1.45.674 1.45 1.506"/>
                </svg>
                Google Play
              </a>
            </div>
          </div>

          <!-- Game 4 -->
          <div class="game-card" ref="gameCard4">
            <div class="game-logo">
              <span class="logo-text">FLIP</span>
              <span class="logo-control">MASTER</span>
            </div>
            <h2 class="game-description">
              Master the art of flipping as you conquer impossible obstacles with style.
            </h2>
            <div class="download-buttons">
              <a href="https://apps.apple.com" class="download-btn app-store" target="_blank">
                <svg class="btn-icon" viewBox="0 0 24 24" fill="currentColor">
                  <path d="M17.05 20.28c-.98.95-2.05.88-3.08.4-1.09-.5-2.08-.48-3.24 0-1.44.62-2.2.44-3.06-.4C2.79 15.25 3.51 7.59 9.05 7.31c1.35.06 2.29.38 3.02.91.99-.72 2.45-.84 3.02-.91 5.54.28 6.26 7.94 2.96 12.97zM12.03 7.25c-.15-2.23 1.66-4.07 3.74-4.25.29 2.58-2.34 4.5-3.74 4.25z"/>
                </svg>
                App Store
              </a>
              <a href="https://play.google.com/store" class="download-btn play-store" target="_blank">
                <svg class="btn-icon" viewBox="0 0 24 24" fill="currentColor">
                  <path d="M3.45 2.426c-.094.148-.15.32-.15.504v7.636h16.8V2.93c0-.184-.056-.356-.15-.504m-16.35.504c0-.832.65-1.506 1.45-1.506h15.1c.8 0 1.45.674 1.45 1.506"/>
                </svg>
                Google Play
              </a>
            </div>
          </div>
        </div>
      </div>

      <!-- Scrollable Content Sections -->
      <div class="games-content-wrapper">
        <!-- Section 1 -->
        <section class="game-section-content" ref="gameSection1">
          <div class="section-inner">
            <h1 class="section-title" ref="gameTitle1">Discover</h1>
            <p class="section-subtitle" ref="gameSubtitle1">Strategy & Control</p>
          </div>
        </section>

        <!-- Section 2 -->
        <section class="game-section-content" ref="gameSection2">
          <div class="section-inner">
            <h1 class="section-title" ref="gameTitle2">Create</h1>
            <p class="section-subtitle" ref="gameSubtitle2">Puzzle & Stack</p>
          </div>
        </section>

        <!-- Section 3 -->
        <section class="game-section-content" ref="gameSection3">
          <div class="section-inner">
            <h1 class="section-title" ref="gameTitle3">Play</h1>
            <p class="section-subtitle" ref="gameSubtitle3">Racing & Rush</p>
          </div>
        </section>

        <!-- Section 4 -->
        <section class="game-section-content" ref="gameSection4">
          <div class="section-inner">
            <h1 class="section-title" ref="gameTitle4">Connect</h1>
            <p class="section-subtitle" ref="gameSubtitle4">Master & Flip</p>
          </div>
        </section>

        <!-- Section 5 -->
        <section class="game-section-content" ref="gameSection5">
          <div class="section-inner">
            <h1 class="section-title" ref="gameTitle5">Explore</h1>
            <p class="section-subtitle" ref="gameSubtitle5">New Adventures</p>
          </div>
        </section>

        <!-- Section 6 -->
        <section class="game-section-content" ref="gameSection6">
          <div class="section-inner">
            <h1 class="section-title" ref="gameTitle6">Achieve</h1>
            <p class="section-subtitle" ref="gameSubtitle6">Endless Fun</p>
          </div>
        </section>
      </div>
    </section>

    <!-- ABOUT SECTION -->
    <section class="about-section" id="about" ref="aboutSection">
      <div class="about-container">
        <h2 class="section-title">About NEKSOLUTION</h2>
        <div class="about-content">
          <p>
            NEKSOLUTION is a leading mobile game publisher creating hyper-casual games that are always fresh, always fun, and always satisfying. With a focus on simple yet addictive gameplay, our games have been downloaded by millions worldwide.
          </p>
          <p>
            We believe in creating gaming experiences that are instantly engaging and endlessly entertaining. From strategy to puzzles, racing to flipping challenges, NEKSOLUTION games offer something for everyone.
          </p>
          <p>
            Join our community of players and discover why NEKSOLUTION games are the most fun you can have on your phone.
          </p>
        </div>
      </div>
    </section>

    <!-- FOOTER SECTION -->
    <footer class="footer-section" ref="footerSection">
      <div class="footer-container">
        <div class="footer-content">
          <h3>NEKSOLUTION Games</h3>
          <p>&copy; 2025 NEKSOLUTION. All rights reserved.</p>
          <div class="footer-links">
            <a href="#" target="_blank">Privacy Policy</a>
            <a href="#" target="_blank">Terms of Service</a>
            <a href="#" target="_blank">Contact Us</a>
          </div>
        </div>
      </div>
    </footer>

    <!-- Scroll Progress -->
    <div class="scroll-progress">
      <div class="progress-bar" ref="progressBar"></div>
    </div>

    <!-- Smooth Scroll Anchor Helper -->
    <div id="home"></div>
    <div id="games"></div>
    <div id="about"></div>
  </div>
</template>

<script>
export default {
  name: 'IndexPage',
  data() {
    return {
      videos: [
        'https://framerusercontent.com/assets/coA6LVNceeKnXPsNbxxTzspCMWQ.mp4',
        'https://framerusercontent.com/assets/J2HtjlxUFqocuEczSgMyv0Se8.mp4',
        'https://framerusercontent.com/assets/OPfeNYS6cWntQd8n6oAbg6AZnFM.mp4',
        'https://framerusercontent.com/assets/EdenTRJNwjluKd054NftzAgjFnU.mp4'
      ],
      currentSection: 'home',
      currentSectionLabel: 'HOME',
      sectionDots: [
        { id: 'home', label: 'Home', active: true },
        { id: 'games', label: 'Games', active: false },
        { id: 'about', label: 'About', active: false }
      ]
    };
  },
  mounted() {
    // Wait for GSAP to load from CDN
    const waitForGSAP = setInterval(() => {
      if (window.gsap && window.ScrollTrigger) {
        clearInterval(waitForGSAP);
        window.gsap.registerPlugin(window.ScrollTrigger);
        
        this.$nextTick(() => {
          this.setupScrollTriggers();
          this.setupProgressBar();
          this.setupSectionTracking();
        });
      }
    }, 100);
    
    // Fallback timeout
    setTimeout(() => {
      clearInterval(waitForGSAP);
    }, 5000);
  },
  methods: {
    scrollToSection(sectionId) {
      const element = document.getElementById(sectionId);
      if (element) {
        element.scrollIntoView({ behavior: 'smooth' });
      }
    },

    setupSectionTracking() {
      const trackScroll = () => {
        const scrollPos = window.scrollY + window.innerHeight / 2;
        const heroSection = this.$refs.heroSection;
        const gamesShowcase = this.$refs.gamesShowcase;
        const aboutSection = this.$refs.aboutSection;

        let currentSection = 'home';
        let sectionLabel = 'HOME';

        if (heroSection && scrollPos < heroSection.offsetHeight) {
          currentSection = 'home';
          sectionLabel = 'HOME';
        } else if (gamesShowcase && scrollPos < heroSection.offsetHeight + gamesShowcase.offsetHeight) {
          currentSection = 'games';
          sectionLabel = 'GAMES';
        } else {
          currentSection = 'about';
          sectionLabel = 'ABOUT';
        }

        this.currentSection = currentSection;
        this.currentSectionLabel = sectionLabel;

        this.sectionDots.forEach((dot, index) => {
          dot.active = dot.id === currentSection;
        });

        // Add/remove visible class to games showcase based on computed opacity
        if (gamesShowcase) {
          const computedStyle = window.getComputedStyle(gamesShowcase);
          const opacity = parseFloat(computedStyle.opacity);
          if (opacity > 0.5) {
            gamesShowcase.classList.add('visible');
          } else {
            gamesShowcase.classList.remove('visible');
          }
        }
      };

      window.addEventListener('scroll', trackScroll, { passive: true });
    },
    setupScrollTriggers() {
      const gsap = window.gsap;
      if (!gsap) return; // GSAP not loaded yet
      
      // Games showcase fade in - START ONLY AFTER HOME 100VH
      if (this.$refs.gamesShowcase && this.$refs.heroSection) {
        gsap.fromTo(this.$refs.gamesShowcase, 
          { opacity: 0 },
          {
            scrollTrigger: {
              trigger: this.$refs.heroSection,
              start: 'bottom bottom',
              end: 'bottom 80%',
              scrub: 0.8,
              markers: false
            },
            opacity: 1,
            duration: 0.8,
            ease: 'power2.out'
          }
        );
      }

      // Hide video when games section ends (text leaves screen)
      if (this.$refs.gamesShowcase && this.$refs.gameSection6) {
        gsap.to(this.$refs.gamesShowcase, 
          {
            scrollTrigger: {
              trigger: this.$refs.gameSection6,
              start: 'bottom 20%',
              end: 'bottom 0%',
              scrub: 0.8,
              markers: false
            },
            opacity: 0,
            duration: 0.8,
            ease: 'power2.in'
          }
        );
      }

      // Game sections scroll triggers
      const gameSections = [
        { ref: this.$refs.gameSection1, titleRef: this.$refs.gameTitle1, subtitleRef: this.$refs.gameSubtitle1, index: 0 },
        { ref: this.$refs.gameSection2, titleRef: this.$refs.gameTitle2, subtitleRef: this.$refs.gameSubtitle2, index: 1 },
        { ref: this.$refs.gameSection3, titleRef: this.$refs.gameTitle3, subtitleRef: this.$refs.gameSubtitle3, index: 2 },
        { ref: this.$refs.gameSection4, titleRef: this.$refs.gameTitle4, subtitleRef: this.$refs.gameSubtitle4, index: 3 },
        { ref: this.$refs.gameSection5, titleRef: this.$refs.gameTitle5, subtitleRef: this.$refs.gameSubtitle5, index: 0 },
        { ref: this.$refs.gameSection6, titleRef: this.$refs.gameTitle6, subtitleRef: this.$refs.gameSubtitle6, index: 1 }
      ];

      gameSections.forEach((section, index) => {
        if (!section.ref) return;

        // Initial state - all hidden
        gsap.set([section.titleRef, section.subtitleRef], {
          opacity: 0,
          y: 50
        });

        // Scroll trigger for content visibility
        gsap.to([section.titleRef, section.subtitleRef], {
          scrollTrigger: {
            trigger: section.ref,
            start: 'top 60%',
            end: 'center 40%',
            scrub: 0.8,
            toggleActions: 'play reverse reverse play',
            onEnter: () => {
              this.switchGameCard(section.index);
              this.changeGameVideo(section.index);
            },
            onEnterBack: () => {
              this.switchGameCard(section.index);
              this.changeGameVideo(section.index);
            }
          },
          opacity: 1,
          y: 0,
          duration: 0.8,
          stagger: 0.08,
          ease: 'power2.out'
        });

        // Fade out when leaving
        gsap.to([section.titleRef, section.subtitleRef], {
          scrollTrigger: {
            trigger: section.ref,
            start: 'bottom 45%',
            end: 'bottom 20%',
            scrub: 0.8,
            toggleActions: 'play reverse reverse play'
          },
          opacity: 0,
          y: -30,
          duration: 0.6,
          ease: 'power2.in'
        });
      });

      // About section animation
      if (this.$refs.aboutSection) {
        gsap.from(this.$refs.aboutSection, {
          scrollTrigger: {
            trigger: this.$refs.aboutSection,
            start: 'top 80%',
            end: 'top 50%',
            scrub: 0.8
          },
          opacity: 0,
          y: 50,
          duration: 0.8
        });
      }
    },

    switchGameCard(index) {
      const gsap = window.gsap;
      if (!gsap) return;
      
      const gameCards = [
        this.$refs.gameCard1,
        this.$refs.gameCard2,
        this.$refs.gameCard3,
        this.$refs.gameCard4
      ];

      gameCards.forEach((card, i) => {
        if (card) {
          if (i === index) {
            card.classList.add('active');
            gsap.to(card, {
              opacity: 1,
              y: 0,
              duration: 0.5,
              ease: 'power2.out'
            });
          } else {
            card.classList.remove('active');
            gsap.to(card, {
              opacity: 0,
              y: 20,
              duration: 0.3,
              ease: 'power2.in',
              pointerEvents: 'none'
            });
          }
        }
      });
    },

    changeGameVideo(index) {
      const gsap = window.gsap;
      if (!gsap) return;
      
      const gamesVideo = this.$refs.gamesVideo;
      if (gamesVideo) {
        // Fade out current video
        gsap.to(gamesVideo, {
          opacity: 0.3,
          duration: 0.5,
          ease: 'power2.inOut'
        });

        // Change source and fade back in
        setTimeout(() => {
          gamesVideo.src = this.videos[index];
          gamesVideo.play().catch(() => {});
          gsap.to(gamesVideo, {
            opacity: 1,
            duration: 0.6,
            ease: 'power2.inOut'
          });
        }, 250);
      }
    },

    setupProgressBar() {
      const scrollListener = () => {
        const docHeight = document.documentElement.scrollHeight - window.innerHeight;
        const scrolled = Math.min((window.scrollY / docHeight) * 100, 100);
        if (this.$refs.progressBar) {
          this.$refs.progressBar.style.width = scrolled + '%';
        }
      };
      window.addEventListener('scroll', scrollListener, { passive: true });
    }
  },
  head() {
    return {
      title: 'NEKSOLUTION - Entertain the World',
      meta: [
        {
          hid: 'description',
          name: 'description',
          content: 'Entertain the world'
        }
      ]
    }
  }
}
</script>

<style scoped>
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
}

.landing-page {
  width: 100%;
  background: #000;
  overflow-x: hidden;
  position: relative;
}

/* Header */
.header-section {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 2000;
  background: rgba(0, 0, 0, 0.98);
  backdrop-filter: blur(15px);
  border-bottom: 1px solid rgba(0, 212, 255, 0.15);
  height: 70px;
  display: flex;
  align-items: center;
  padding: 0 2rem;
  box-shadow: 0 4px 30px rgba(0, 0, 0, 0.3);
}

.navbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  width: 100%;
}

.logo {
  font-size: 1.5rem;
  font-weight: 700;
  color: #fff;
  letter-spacing: 2px;
  background: linear-gradient(135deg, #00d4ff 0%, #0099ff 100%);
  background-clip: text;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

.nav-links {
  display: flex;
  gap: 2.5rem;
}

.nav-link {
  color: rgba(255, 255, 255, 0.7);
  text-decoration: none;
  font-weight: 500;
  font-size: 0.95rem;
  transition: all 0.3s ease;
  position: relative;
}

.nav-link::after {
  content: '';
  position: absolute;
  bottom: -5px;
  left: 0;
  width: 0;
  height: 2px;
  background: #00d4ff;
  transition: width 0.3s ease;
}

.nav-link:hover {
  color: #00d4ff;
}

.nav-link:hover::after {
  width: 100%;
}

/* Section Navigation Dots */
.section-nav-dots {
  position: fixed;
  right: 30px;
  top: 50%;
  transform: translateY(-50%);
  z-index: 1800;
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
}

.nav-dot {
  width: 14px;
  height: 14px;
  border-radius: 50%;
  border: 2px solid rgba(0, 212, 255, 0.4);
  background: transparent;
  cursor: pointer;
  transition: all 0.4s ease;
  position: relative;
}

.nav-dot::before {
  content: '';
  position: absolute;
  width: 20px;
  height: 20px;
  border-radius: 50%;
  border: 2px solid rgba(0, 212, 255, 0.2);
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  opacity: 0;
  transition: all 0.4s ease;
}

.nav-dot:hover {
  border-color: rgba(0, 212, 255, 0.8);
}

.nav-dot:hover::before {
  opacity: 1;
  width: 28px;
  height: 28px;
}

.nav-dot.active {
  background: #00d4ff;
  border-color: #00d4ff;
  box-shadow: 0 0 20px rgba(0, 212, 255, 0.6), inset 0 0 10px rgba(0, 212, 255, 0.3);
}

.nav-dot.active::before {
  opacity: 1;
  width: 28px;
  height: 28px;
  border-color: rgba(0, 212, 255, 0.4);
}

/* Section Label */
.section-label {
  position: fixed;
  left: 50%;
  top: 100px;
  transform: translateX(-50%);
  z-index: 1800;
  pointer-events: none;
}

.label-text {
  font-size: 0.85rem;
  font-weight: 700;
  letter-spacing: 3px;
  color: rgba(0, 212, 255, 0.6);
  text-shadow: 0 0 20px rgba(0, 212, 255, 0.3);
  animation: labelPulse 3s ease-in-out infinite;
}

@keyframes labelPulse {
  0%, 100% {
    opacity: 0.5;
  }
  50% {
    opacity: 1;
  }
}

/* HERO SECTION */
.hero-section {
  position: fixed;
  top: 70px;
  left: 0;
  width: 100%;
  height: calc(100vh - 70px);
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
  z-index: 4;
  scroll-snap-align: start;
}

.fixed-video-container {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 1;
  overflow: hidden;
  pointer-events: none;
}

.bg-video {
  width: 100%;
  height: 100%;
  object-fit: cover;
  filter: brightness(0.8);
}

.video-overlay {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: linear-gradient(135deg, rgba(0, 0, 0, 0.6) 0%, rgba(0, 0, 0, 0.4) 50%, rgba(0, 0, 0, 0.6) 100%);
  z-index: 2;
  pointer-events: none;
}

.hero-content {
  position: relative;
  z-index: 10;
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
}

.hero-text-center {
  text-align: center;
  max-width: 800px;
  padding: 2rem;
}

.hero-title {
  font-size: 4.5rem;
  font-weight: 700;
  color: #fff;
  margin-bottom: 2rem;
  text-shadow: 0 6px 30px rgba(0, 212, 255, 0.4), 0 12px 60px rgba(0, 0, 0, 0.8);
  letter-spacing: -1px;
  line-height: 1.2;
  animation: titleSlideIn 0.8s ease-out;
  background: linear-gradient(135deg, #fff 0%, #00d4ff 100%);
  background-clip: text;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

.hero-description {
  font-size: 1.4rem;
  color: rgba(255, 255, 255, 0.9);
  line-height: 1.8;
  text-shadow: 0 2px 15px rgba(0, 0, 0, 0.8);
  animation: descSlideIn 0.8s ease-out 0.1s both;
  font-weight: 300;
  letter-spacing: 0.5px;
}

@keyframes titleSlideIn {
  from {
    opacity: 0;
    transform: translateX(-30px);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}

@keyframes descSlideIn {
  from {
    opacity: 0;
    transform: translateX(-20px);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}

/* GAMES SHOWCASE SECTION */
.games-showcase {
  background: #000;
  position: relative;
  z-index: 5;
  display: flex;
  min-height: 600vh;
  scroll-snap-align: start;
  opacity: 0;
  pointer-events: none;
  transition: pointer-events 0.3s ease;
}

.games-showcase.visible {
  pointer-events: auto;
}

.games-fixed-video-container {
  position: fixed;
  top: 70px;
  left: 0;
  width: 100%;
  height: calc(100vh - 70px);
  z-index: 1;
  overflow: hidden;
  pointer-events: none;
}

.games-sidebar-panel {
  position: fixed;
  left: 0;
  top: 70px;
  width: 380px;
  height: calc(100vh - 70px);
  background: linear-gradient(135deg, rgba(0, 0, 0, 0.85) 0%, rgba(20, 20, 40, 0.75) 100%);
  backdrop-filter: blur(30px);
  border-right: 2px solid rgba(0, 212, 255, 0.25);
  z-index: 50;
  display: flex;
  align-items: flex-start;
  justify-content: flex-start;
  overflow-y: auto;
  padding: 4.5rem 2.5rem;
  box-shadow: -20px 0 60px rgba(0, 212, 255, 0.1);
}

.games-sidebar-content {
  width: 100%;
  position: relative;
}

.game-card {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  opacity: 0;
  y: 20px;
  pointer-events: none;
  transition: pointer-events 0.3s ease;
  will-change: opacity, transform;
}

.game-card.active {
  pointer-events: auto;
}

.game-logo {
  display: flex;
  flex-direction: column;
  gap: -0.5rem;
  margin-bottom: 2.5rem;
  font-weight: 900;
  font-size: 3.2rem;
  letter-spacing: -1px;
  line-height: 0.9;
}

.logo-text {
  color: #00d4ff;
  text-shadow: 0 0 30px rgba(0, 212, 255, 0.9), 0 0 60px rgba(0, 212, 255, 0.4), 2px 2px 0 rgba(0, 0, 0, 0.8);
  font-family: 'Arial Black', sans-serif;
}

.logo-control {
  background: linear-gradient(90deg, #ffed4e 0%, #ffc300 100%);
  background-clip: text;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  text-shadow: none;
  font-family: 'Arial Black', sans-serif;
  letter-spacing: 2px;
  filter: drop-shadow(0 0 15px rgba(255, 195, 0, 0.5));
}

.game-description {
  font-size: 1.35rem;
  font-weight: 600;
  color: #fff;
  margin-bottom: 2.5rem;
  line-height: 1.6;
  text-shadow: 0 4px 20px rgba(0, 212, 255, 0.2), 0 8px 30px rgba(0, 0, 0, 0.8);
}

.games-content-wrapper {
  position: relative;
  z-index: 10;
  width: 100%;
  margin-left: 380px;
}

.game-section-content {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 4rem 2rem;
  position: relative;
  z-index: 10;
  backdrop-filter: rgba(0, 0, 0, 0.3);
  scroll-snap-align: center;
}

.section-inner {
  text-align: center;
  max-width: 900px;
  will-change: opacity, transform;
}

.section-title {
  font-size: 7rem;
  font-weight: 700;
  color: #fff;
  line-height: 1;
  margin-bottom: 1.5rem;
  text-shadow: 0 8px 40px rgba(0, 212, 255, 0.3), 0 12px 50px rgba(0, 0, 0, 0.8);
  letter-spacing: -2px;
  will-change: opacity, transform;
  background: linear-gradient(135deg, #fff 0%, #00d4ff 50%, #fff 100%);
  background-size: 200% 200%;
  background-clip: text;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

.section-subtitle {
  font-size: 4rem;
  font-weight: 700;
  color: #ffc300;
  line-height: 1.2;
  text-shadow: 0 4px 20px rgba(255, 195, 0, 0.4), 0 8px 30px rgba(0, 0, 0, 0.8);
  letter-spacing: -1px;
  will-change: opacity, transform;
}

.btn-icon {
  width: 24px;
  height: 24px;
  flex-shrink: 0;
}

.download-buttons {
  display: flex;
  flex-direction: column;
  gap: 1.2rem;
}

.download-buttons.sm {
  flex-direction: row;
  gap: 0.8rem;
}

.download-btn {
  display: flex;
  align-items: center;
  justify-content: flex-start;
  gap: 0.8rem;
  padding: 1rem 1.3rem;
  border: 2px solid rgba(0, 212, 255, 0.3);
  border-radius: 8px;
  background: rgba(255, 255, 255, 0.08);
  color: #fff;
  font-weight: 600;
  font-size: 0.95rem;
  text-decoration: none;
  transition: all 0.3s ease;
  cursor: pointer;
  backdrop-filter: blur(15px);
  text-shadow: 0 2px 8px rgba(0, 0, 0, 0.8);
}

.download-btn:hover {
  background: rgba(0, 212, 255, 0.1);
  border-color: #00d4ff;
  box-shadow: 0 0 20px rgba(0, 212, 255, 0.4), inset 0 0 10px rgba(0, 212, 255, 0.1);
  transform: translateY(-3px);
}

/* Old Games Grid Styles (removed) */
.games-container {
  display: none;
}

.game-item {
  display: none;
}

.game-preview {
  display: none;
}

.game-video {
  display: none;
}

.game-info {
  display: none;
}

.game-desc {
  display: none;
}

/* ABOUT SECTION */
.about-section {
  background: linear-gradient(135deg, rgba(20, 20, 40, 0.8) 0%, rgba(0, 0, 0, 0.9) 100%);
  padding: 8rem 2rem;
  position: relative;
  z-index: 5;
  border-top: 1px solid rgba(0, 212, 255, 0.15);
  scroll-snap-align: start;
}

.about-container {
  max-width: 900px;
  margin: 0 auto;
}

.about-section .section-title {
  font-size: 3rem;
  font-weight: 700;
  color: #fff;
  margin-bottom: 3rem;
  text-align: center;
  text-shadow: 0 4px 20px rgba(0, 212, 255, 0.2), 0 8px 30px rgba(0, 0, 0, 0.8);
  background: linear-gradient(135deg, #fff 0%, #00d4ff 100%);
  background-clip: text;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

.about-content {
  display: flex;
  flex-direction: column;
  gap: 2.5rem;
}

.about-content p {
  font-size: 1.15rem;
  color: rgba(255, 255, 255, 0.85);
  line-height: 1.9;
  text-shadow: 0 2px 10px rgba(0, 0, 0, 0.8);
  transition: color 0.3s ease;
}

.about-content p:first-letter {
  color: #00d4ff;
  font-weight: 700;
}

/* FOOTER SECTION */
.footer-section {
  background: rgba(0, 0, 0, 0.98);
  border-top: 2px solid rgba(0, 212, 255, 0.15);
  padding: 4rem 2rem;
  position: relative;
  z-index: 5;
  box-shadow: 0 -4px 30px rgba(0, 0, 0, 0.3);
}

.footer-container {
  max-width: 1400px;
  margin: 0 auto;
}

.footer-content {
  text-align: center;
}

.footer-content h3 {
  font-size: 1.5rem;
  color: #fff;
  margin-bottom: 1rem;
  text-shadow: 0 2px 10px rgba(0, 0, 0, 0.8);
  background: linear-gradient(135deg, #00d4ff 0%, #0099ff 100%);
  background-clip: text;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  font-weight: 700;
}

.footer-content p {
  color: rgba(255, 255, 255, 0.6);
  margin-bottom: 2rem;
  font-size: 0.95rem;
}

.footer-links {
  display: flex;
  justify-content: center;
  gap: 2.5rem;
  flex-wrap: wrap;
}

.footer-links a {
  color: rgba(255, 255, 255, 0.7);
  text-decoration: none;
  transition: all 0.3s ease;
  position: relative;
  font-weight: 500;
}

.footer-links a::after {
  content: '';
  position: absolute;
  bottom: -5px;
  left: 0;
  width: 0;
  height: 2px;
  background: #00d4ff;
  transition: width 0.3s ease;
}

.footer-links a:hover {
  color: #00d4ff;
}

.footer-links a:hover::after {
  width: 100%;
}

/* Scroll Progress */
.scroll-progress {
  position: fixed;
  top: 70px;
  left: 0;
  width: 100%;
  height: 4px;
  background: rgba(255, 255, 255, 0.08);
  z-index: 1999;
  pointer-events: none;
  box-shadow: 0 0 20px rgba(0, 212, 255, 0.1);
}

.progress-bar {
  height: 100%;
  background: linear-gradient(90deg, #00d4ff 0%, #00ff88 50%, #0099ff 100%);
  width: 0%;
  box-shadow: 0 0 20px rgba(0, 212, 255, 0.8), inset 0 0 10px rgba(0, 212, 255, 0.4);
  transition: width 0.1s linear;
}

/* Responsive */
@media (max-width: 1024px) {
  .hero-title {
    font-size: 3.5rem;
  }

  .hero-description {
    font-size: 1.2rem;
  }

  .games-sidebar-panel {
    width: 320px;
    padding: 4rem 2rem;
  }

  .games-content-wrapper {
    margin-left: 320px;
  }

  .section-title {
    font-size: 5.5rem;
  }

  .section-subtitle {
    font-size: 3.2rem;
  }

  .section-nav-dots {
    right: 20px;
    gap: 1.2rem;
  }
}

@media (max-width: 768px) {
  .hero-section {
    position: relative;
    height: auto;
    min-height: 100vh;
    margin-top: 70px;
    justify-content: center;
  }

  .hero-title {
    font-size: 2.5rem;
    margin-bottom: 1.5rem;
  }

  .hero-description {
    font-size: 1.1rem;
  }

  .hero-text-center {
    padding: 2rem 1.5rem;
  }

  .games-showcase {
    min-height: auto;
    flex-direction: column;
    margin-top: 0;
  }

  .games-fixed-video-container {
    position: relative;
    height: 300px;
    top: auto;
  }

  .games-sidebar-panel {
    position: static;
    width: 100%;
    height: auto;
    border-right: none;
    border-bottom: 2px solid rgba(0, 212, 255, 0.2);
    padding: 2.5rem 2rem;
  }

  .games-content-wrapper {
    margin-left: 0;
    min-height: auto;
  }

  .game-section-content {
    min-height: 60vh;
    padding: 2.5rem 1.5rem;
  }

  .section-title {
    font-size: 3rem;
  }

  .section-subtitle {
    font-size: 2rem;
  }

  .section-nav-dots {
    display: none;
  }

  .section-label {
    display: none;
  }

  .nav-links {
    gap: 1rem;
    font-size: 0.9rem;
  }
}

@media (max-width: 480px) {
  .hero-section {
    position: relative;
    height: auto;
    min-height: 100vh;
    margin-top: 70px;
  }

  .hero-title {
    font-size: 2rem;
    margin-bottom: 1rem;
  }

  .hero-description {
    font-size: 1rem;
  }

  .hero-text-center {
    padding: 2rem 1.2rem;
  }

  .section-title {
    font-size: 2rem;
  }

  .section-subtitle {
    font-size: 1.4rem;
  }

  .games-sidebar-panel {
    padding: 1.5rem 1.2rem;
  }

  .game-logo {
    font-size: 2.2rem;
    margin-bottom: 1.5rem;
  }

  .game-description {
    font-size: 1rem;
    margin-bottom: 1.5rem;
  }

  .download-buttons {
    gap: 0.8rem;
  }

  .download-btn {
    padding: 0.8rem 1rem;
    font-size: 0.85rem;
  }

  .about-section {
    padding: 4rem 1.5rem;
  }

  .about-section .section-title {
    font-size: 1.8rem;
  }

  .about-content p {
    font-size: 0.95rem;
    line-height: 1.6;
  }

  .footer-section {
    padding: 2rem 1.5rem;
  }

  .footer-content h3 {
    font-size: 1.2rem;
  }

  .footer-links {
    gap: 1.5rem;
    flex-direction: column;
  }

  .footer-links a {
    display: block;
  }

  .nav-links {
    gap: 0.8rem;
    font-size: 0.8rem;
  }

  .logo {
    font-size: 1.2rem;
  }
}
</style>
